> AI-generated note: This document was generated by AI and may contain mistakes.

JSON Explorer is an interactive web tool that deeply analyses any JSON document and gives a comprehensive structural overview. Paste JSON directly or drag-and-drop a file; the tool walks the entire document and collects per-path statistics. It presents results in four sections: a summary dashboard (root type, total unique paths, max nesting depth, and per-type node counts); an interactive collapsible structure tree where every node shows its JSONPath, type badge(s), and inline statistics — objects list their immediate keys, arrays show item-count ranges, strings show unique-value count and average length with the three most-frequent values highlighted, numbers show min/max/mean, and booleans show true/false percentages; a flat path table listing every path with occurrence count, null count, and stats; and a copyable Markdown context report suitable for sharing or pasting into an LLM prompt. Mixed-type fields (e.g. a field that is sometimes a number and sometimes null) are detected and shown with multiple type badges. JSONL files (one JSON object per line) are automatically detected and treated as an array.

<!-- Generated from commit: initial -->
